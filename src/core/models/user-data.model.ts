export type UserData = {
  token: string;
  id: string;
  name: string;
  lastName: string;
  email: string;
  avatar?: string;
};

export type PayloadData = {
  id: string;
  name: string;
  lastName: string;
  email: string;
  avatar?: string;
};
